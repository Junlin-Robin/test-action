name: Quick-man_Deploy
run-name: ${{ github.actor }} is action

on:
  push:
    branches:
      - master
    tags:
      - v*

jobs:
   deploy:
    runs-on: ubuntu-latest
    steps:
      - name: print-start
        run: echo "start"

      - name: checkout
        uses: actions/checkout@v3

      - run: echo '111'

      - name: print-end
        run: echo "end"
